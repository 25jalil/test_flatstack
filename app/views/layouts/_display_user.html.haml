- if signed_in?
  = link_to current_user.email, user_path(current_user), id: "pozition1"
  = link_to "Sign out", signout_path, method: "delete", id: "pozition2"
- else
  %p= link_to "Sign in", signin_path, id: "pozition1"
  %p= link_to "Registration", new_user_path, id: "pozition2"

